# Выводим содержимое всех файлов с расширением .log
# Оставляем только строки, содержащие "error"
# Создаём сжатый архив из отфильтрованных и отсортированных строк
cat *.log | grep "error" | awk '{print $0}' | sort | tar -czf filtered_logs.tar.gz -T
